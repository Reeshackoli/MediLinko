# MediLinko Backend Environment Configuration
# Copy this file to .env and update with your values

# ================================
# MongoDB Configuration
# ================================
# Option 1: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/medilinko

# Option 2: MongoDB Atlas (recommended)
# Replace <username>, <password>, and <cluster> with your values
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/medilinko?retryWrites=true&w=majority

# ================================
# JWT Configuration
# ================================
# Generate a strong random secret for production
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-strong-random-secret-in-production
JWT_EXPIRE=7d

# ================================
# Server Configuration
# ================================
PORT=3000
NODE_ENV=development
HOST=0.0.0.0

# ================================
# CORS Configuration
# ================================
# For development, allow all origins
CORS_ORIGIN=*

# For production, specify your frontend domain
# CORS_ORIGIN=https://yourdomain.com

# ================================
# Firebase Configuration
# ================================
# Path to Firebase service account JSON file (relative to backend folder)
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# ================================
# Optional: Logging
# ================================
# LOG_LEVEL=debug

# ================================
# Optional: Rate Limiting
# ================================
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
